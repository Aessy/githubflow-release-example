Task Priorities
===============
Priority field added to task model:
  - priority  enum: critical | high | normal | low  (default: normal)

API changes:
  - POST /tasks accepts optional "priority" field
  - PATCH /tasks/:id accepts "priority" in update body
  - GET /tasks supports ?priority=high filter (repeatable for multiple values)
  - Task response object now includes "priority" field
