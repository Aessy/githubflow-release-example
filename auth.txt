Authentication
==============
User model:
  - id, email, password (bcrypt cost 12), created_at

Endpoints:
  POST /auth/login    — returns signed JWT (1h expiry)
  POST /auth/refresh  — issues new token, invalidates old
  POST /auth/logout   — adds token to deny-list

Rate limiting:
  - Login attempts capped at 10 per IP per minute
  - Excess requests receive 429 Too Many Requests

HOTFIX (v1.0.0.1):
  Token expiry was incorrectly calculated using server local time instead
  of UTC. Tokens were effectively non-expiring on servers not in UTC.
  Fix: all exp/iat claims now use UTC epoch seconds (time.time()).
  Affected endpoint: POST /auth/login, POST /auth/refresh

Error codes:
  400  malformed request body
  401  invalid credentials or expired token
  422  missing required fields
  429  rate limit exceeded
